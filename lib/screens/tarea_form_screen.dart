import 'package:flutter/material.dart';

class TareaFormScreen extends StatelessWidget {
  const TareaFormScreen({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO EXAMEN:
    // - crear / editar tarea
    // - validaciones
    return Scaffold(
      appBar: AppBar(title: const Text('Formulario Tarea')),
      body: Container(),
    );
  }
}
